POSTGRES_DB=intellimix
POSTGRES_USER=intellimix
POSTGRES_PASSWORD=replace-with-strong-password

FLASK_SECRET_KEY=replace-with-strong-random-value
JWT_SECRET_KEY=replace-with-strong-random-value
GOOGLE_API_KEY=replace-with-gemini-api-key
GEMINI_MODEL_NAME=gemini-3-flash-preview
GEMINI_MAX_RETRIES=2
GEMINI_RETRY_BASE_SECONDS=2

FRONTEND_ORIGIN=http://localhost:8080
FRONTEND_PORT=8080
BACKEND_PORT=5000

# Hot-reload dev stack ports (docker-compose.dev.yml)
DEV_FRONTEND_PORT=5173
DEV_BACKEND_PORT=5001

JWT_ACCESS_TOKEN_MINUTES=30
JWT_REFRESH_TOKEN_DAYS=30
MAX_UPLOAD_SIZE_MB=100

# Keep this empty to use same-origin API through Nginx proxy.
VITE_API_URL=
